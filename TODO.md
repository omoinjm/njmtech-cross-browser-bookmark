# TODO

- [x] Set `BOOKMARK_ENCRYPTION_KEY` manually in Cloudflare/Wrangler before deployment, and keep the same value permanently for each environment so existing encrypted bookmarks remain decryptable.
