{
  "name": "bookmark-sync-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute bookmarks-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute bookmarks-db --remote --file=./schema.sql",
    "test:extension": "playwright test",
    "lint:firefox": "[ -f extension/config.js ] || cp extension/config.example.js extension/config.js; web-ext lint --source-dir=extension",
    "package:firefox": "[ -f extension/config.js ] || cp extension/config.example.js extension/config.js; web-ext build --source-dir=extension --artifacts-dir=web-ext-artifacts --ignore-files config.example.js --overwrite-dest",
    "publish:firefox": "[ -f extension/config.js ] || cp extension/config.example.js extension/config.js; web-ext sign --source-dir=extension --artifacts-dir=web-ext-artifacts --ignore-files config.example.js --channel=listed"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@playwright/test": "^1.62.1",
    "typescript": "^5.7.2",
    "web-ext": "^10.6.0",
    "wrangler": "^3.99.0"
  }
}
