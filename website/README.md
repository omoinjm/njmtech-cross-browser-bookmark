# website/

Static marketing site for Bookmark Sync Engine — plain HTML/CSS/JS, no build step. Deployed to GitHub Pages by `.github/workflows/deploy-pages.yml` on every push to `main` that touches this folder, at the custom domain in `CNAME` (`bookmark.njmtech.co.za`).
